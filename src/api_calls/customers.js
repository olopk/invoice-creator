// PRODUCTS

export const add_customer = (id) => {
    // even more magic with the DELETE call send to the server
}

export const update_customer = (id) => {
    // even more magic with the DELETE call send to the server
}

export const delete_customer = (id) => {
    // even more magic with the DELETE call send to the server
}